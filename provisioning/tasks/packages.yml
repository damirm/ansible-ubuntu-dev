---
- name: Update apt
  apt:
    update_cache: yes

- name: Install packages
  become: true
  apt:
    name: "{{ item }}"
  loop:
    - build-essential
    - tmux
    - unzip
    - curl
    - wget
    - wrk
    - apache2-utils
    - htop
    - make
    - gcc
